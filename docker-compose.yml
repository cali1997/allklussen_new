version: '3.8'
services:
  web:
    build: .
    container_name: allklussen_web
    expose:
      - "8086"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=production
    command: flask run --host=0.0.0.0 --port=8086
